<!--Displys a welecome page for the CloudTask application-->
<!--shows diffrent actions depending on whether the user is signed in-->
<!--provides quick access to posts and logout for authenticated users-->
<!--provides login and sign up options for unauthenticated visitors-->
<div class="container" style="text-align:center; margin-top: 50px;">
  <h1>Welcome to CloudTask</h1>
  <p>Your simple and secure cloud-based task management system.</p>

  <div style="margin-top: 20px;">

    <% if user_signed_in? %>
      <p>you are signed in as <strong><%= current_user.email %></strong>.</p>

      <%= link_to "view Posts", posts_path, class: "btn btn-primary" %>
      <%= button_to "Log Out", destroy_user_session_path,
                method: :delete,
                class: "btn btn-danger",
                form: { style: "display:inline;" } %>

    <% else %>
      <%= link_to "Login", new_user_session_path, class: "btn btn-primary" %>
      <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-success", style: "margin-left: 10px;" %>
    <% end %>

  </div>
</div>
